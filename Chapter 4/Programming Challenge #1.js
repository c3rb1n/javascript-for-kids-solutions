const scores = {
    john: 0,
    pete: 14,
    jack: 6
};

// Increase John's scores by 30
scores.john += 10;

// Increase Pete's scores by 10
scores.pete += 20;

// Increase Jack's scores by 20
scores.jack += 30;

// Show scores
scores; // {john: 10, pete: 34, jack: 36}
